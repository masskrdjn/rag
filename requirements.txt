# API
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0

# RAG / LangChain
langchain>=0.3
langchain-community>=0.3
langchain-chroma>=0.1
langchain-ollama>=0.2
langchain-text-splitters>=0.3

# Vector store
chromadb>=0.5

# Ingestion HTML
beautifulsoup4>=4.12

# Tests / clients HTTP
requests>=2.31

# Optionnel — uniquement si RAG_USE_GPU=1 (reranker_gpu, hallucination_detector_gpu)
# torch>=2.1
# sentence-transformers>=2.7
